# publicacao

Docker Compose na prática!

Hoje avancei meus estudos em Docker Compose, entendendo como orquestrar múltiplos containers de forma simples e organizada usando apenas um arquivo docker-compose.yml.

Principais aprendizados deste capítulo:

✅ O que é o Docker Compose e por que ele é essencial para aplicações multicontainer
✅ Diferença entre Docker Compose V1 e V2 (docker-compose vs docker compose)
✅ Criação de serviços com Node.js, MongoDB e Nginx
✅ Uso de depends_on com healthcheck para garantir a ordem correta de inicialização
✅ Persistência de dados com volumes e agilidade no desenvolvimento com bind mounts
✅ Gerenciamento de variáveis com environment e env_file
✅ Criação de redes personalizadas para isolar serviços (backend, db e frontend)
✅ Acesso ao host usando extra_hosts e host.docker.internal
✅ Introdução ao Compose Watch para sincronizar arquivos em tempo real durante o desenvolvimento

O Docker Compose facilita muito a criação de ambientes completos, reproduzíveis e escaláveis, trazendo organização e produtividade para o dia a dia de quem trabalha com containers e cloud.

Cada capítulo reforça meu foco em me tornar especialista em Docker, Containers e Cloud Computing ☁️🐳
Seguimos evoluindo um passo por dia!

#Docker #DockerCompose #CloudComputing #DevOps #Containers #NodeJS #MongoDB #Nginx #AprendizadoContínuo #Tech #FullCycle #Estudos

# comandos

docker compose up -d
docker compose up -d --build
apt-get install iputils-ping
docker compose up --watch
docker compose down -v
